# Test Files

## JSON
- `valid_large.json` — 178-line config, fully valid
- `bad_large.json` — Contains: unclosed array, mismatch `)`→`]`, unclosed object, unexpected `]`, unclosed tags

## HTML
- `valid_large.html` — 186-line dashboard page
- `bad_large.html` — Contains: 5 mismatched closers, 2 unclosed tags

## Math
- `valid_large.txt` — Complex expressions (area, energy, matrices)
- `bad_large.txt` — Contains: unclosed `)`, unexpected `]`, double operator `+*`, trailing `*`, missing operator, unclosed `))`